document.addEventListener('DOMContentLoaded',()=>{const search=document.querySelector('#doctorSearch'),spec=document.querySelector('#specialization'),dept=document.querySelector('#departmentFilter'),cards=[...document.querySelectorAll('[data-doctor]')];if(!cards.length)return;const filter=()=>{const q=(search?.value||'').toLowerCase(),s=spec?.value||'',d=dept?.value||'';cards.forEach(c=>{const text=c.textContent.toLowerCase();const ok=(!q||text.includes(q))&&(!s||c.dataset.specialization===s)&&(!d||c.dataset.department===d);c.style.display=ok?'':'none'})};[search,spec,dept].forEach(x=>x?.addEventListener('input',filter));});
